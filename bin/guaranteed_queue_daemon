#!/bin/bash

case $1 in
    start)
       echo $$ > $2/tmp/pids/guaranteed_queue.pid;
       exec 2>&1 bundle exec guaranteed_queue 1>log/guaranteed_queue.log
       ;;
     stop)  
       kill `$2/tmp/pids/guaranteed_queue.pid`;;
     *)  
       echo "usage: xyz {start|stop}" ;;
esac
exit 0
